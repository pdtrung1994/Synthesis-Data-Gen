Bootstrap: docker
From: ubuntu:22.04

%post
    export DEBIAN_FRONTEND=noninteractive
    apt-get -y update
    apt-get -y install python3 python3-pip curl
    
    # Install dependencies from requirements.txt
    pip3 install numpy pandas scikit-learn scipy imbalanced-learn matplotlib seaborn ctgan hmmlearn tqdm
